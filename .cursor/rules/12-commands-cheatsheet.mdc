---
alwaysApply: false
---

### Commands cheatsheet

- Dev: `yarn dev` (Next dev server). Run `yarn prebuild` first if `public/sw-template.js` changed.
- Build: `yarn build` (Next build only). Run `yarn prebuild` beforehand when the SW template changes. Env-specific builds: `yarn build:development|staging|production` (these already run prebuild).
- Start (prod build): `yarn start`
- Analyze: `yarn analyze` | `yarn analyze:server` | `yarn analyze:browser`
- Lint: `yarn lint`
- Typecheck: `yarn typecheck`
- Unit tests: `yarn test` (or `yarn test:watch`)
- Coverage: `yarn test:coverage`
- E2E: `yarn test:e2e` (UI: `yarn test:e2e:ui`, debug: `yarn test:e2e:debug`)
- React Scan (perf insights): `yarn scan` (visit `localhost:3000` first)
